#include "main.h"
/**
 * printPercent- Prints percentage
 * @orodha: String argument
 *
 *
 * Return: number of characters
 */

int printPercent(va_list orodha)
{
	int i = 0;

	i = putChar('%');

	return (i);
}
